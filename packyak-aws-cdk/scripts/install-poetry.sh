#!/bin/bash


pipx
poetry

# options
pyenv # compiles python, breaks

conda # downloads everything, but has assumptions around the environment, but mostly go away if you're using poetry
minconda # 
# poetry installs in conda by default if you have the conda environment activated

# EFS with pyenv, do compilation once.

# cluster

# baked in AMI